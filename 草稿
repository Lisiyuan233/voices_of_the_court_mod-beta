can_talk_in_tavern_east = {
	AND = {
		scope:actor = {is_imprisoned = no}
		scope:recipient = {is_imprisoned = no}
		scope:actor.location.culture = { has_coa_gfx = chinese_group_coa_gfx }
		or = {
			scope:actor.location = { has_building_or_higher = castle_01 }
			scope:actor.location = { has_building_or_higher = city_01 }
			scope:actor.location = { has_building_or_higher = temple_01 }
		}
	}
}


create_artifact = {
	name = votc_huixin_title4
	description = ""
	type = miscellaneous
	visuals = scroll
	creator = global_var:message_second_scope_letter_4 
						}
create_artifact	创建一件新宝物并将其加入指定角色的物品栏	
注意：我们隐式地使用了当前作用域。相关配置位于 common/artifacts/visuals
name = 动态描述 - 宝物名称
description = 动态描述 - 宝物描述
rarity = 枚举 - 宝物稀有度，例如 legendary
type = 标记 - 物品栏槽位类型，例如 trinket
modifier = 静态修正 - 作用于佩戴该宝物的角色
durability = 脚本值 - 新耐久度，默认即为最大值
max_durability = 脚本值 - 可选。最大耐久度值，会覆盖 defines 中的默认值
decaying = yes/no - 可选。是否随时间衰减，默认 yes
history = 宝物历史条目 - 自定义历史条目，例如表明该宝物由非所有者重铸
   type = 宝物历史条目类型 - 可用类型：
    created_before_history
    created
    discovered
    claimed_by_house
    given
    stolen
    inherited
    conquest
    taken_in_siege
    taken_in_battle
    won_in_duel
    reforged
template = 宝物脚本模板 - 带触发器与修正的脚本基础模板
visuals = 宝物视觉类型 - 宝物在视觉上的呈现方式
generate_history = 布尔值 - 若未手动编写历史，是否自动生成新历史条目？
quality = 脚本值 - 新品质，用于 AI 评分
wealth = 脚本值 - 新财富值，用于 AI 评分
creator = 角色作用域 - 设置宝物的自定义创建者（默认是所有者）
visuals_source = 含领地头衔、家族或王朝的作用域 - 设置宝物纹章图形的来源
（仅少数宝物模型使用，最典型的是旗帜）
 save_scope_as = 新宝物 - 可选方式，获取新建宝物的引用
title_history = 头衔 - 将指定头衔的历史条目加入宝物历史
title_history_date = 游戏日期 - 从该日期起复制指定头衔的历史条目
creator = 角色作用域 - 设置宝物的自定义创建者（默认是所有者）

输入框



多个信件管理
设置两个变量，一个变量组用于识别递送线程是否占用，一个变量用于存储当前线程的信件编号

set_global_variable = votc_letter_1
set_global_variable = votc_letter_2
set_global_variable = votc_letter_3
    	
		#当变量letter1不存在时，设置letter1为当前线程
if = {
    limit = { 
		not = {exists = var:votc_letter_1 }
		}
		set_global_variable = votc_letter_1
		set_global_variable = {
			name = votc_letter_num
			value = flag:letter_1
			}
}
else if = {
    limit = { 
		not = {exists = var:votc_letter_2 }
		}
    # 当变量letter2不存在时，设置letter2为当前线程
		set_global_variable = votc_letter_2
		set_global_variable = {
			name = votc_letter_num
			value = flag:letter_2
			}
}
else = {
    limit = { 
		not = {exists = var:votc_letter_3 }
		}
    # 当变量letter3不存在时，设置letter3为当前线程
		set_global_variable = votc_letter_3
		set_global_variable = {
			name = votc_letter_num
			value = flag:letter_3
			}
}




			set_global_variable = {
				name = votc_letter_num
				value = flag:letter_1
			}
		remove_variable = test_saved_text


[ROOT.Char.GetLocalizedText( 'story_cycle_dog_name' )]
储存文本
debug.360 = {
	type = character_event
	title = enter_text_save_in_death_title
	desc = enter_text_save_in_death_desc
	theme = default
	left_portrait = root
	orphan = yes

	widget = {
		gui = "event_window_widget_enter_text"
		container = "dynamic_birth_name"
		controller = {
			type = text
			data = {
				key = test_saved_text
				default = cat_name_mite
			}
		}
		setup_scope = {
			root = { save_scope_as = text_target }
		}
	}

	option = {
		name = enter_text_save_in_death_opt
		store_localized_text_in_death = test_saved_text
		add_to_global_variable_list = {
			name = test_saved_text_character
			target = root
		}
		remove_variable = test_saved_text
		death = natural
	}
}

debug.361 = {
	type = character_event
	title = enter_text_save_in_death_title
	desc = enter_text_save_in_death_retreive_desc
	theme = default
	left_portrait = scope:text_character
	orphan = yes

	immediate = {
		random_in_global_list = {
			variable = test_saved_text_character
			save_scope_as = text_character
		}
	}

	option = {
		name = enter_text_save_in_death_retreive_opt
	}
	option = {
		name = enter_text_save_in_death_clear_opt
		every_in_global_list = {
			variable = test_saved_text_character
			remove_localized_text = test_saved_text
		}
	}
}